"""Спільна конфігурація pytest.

test_unittest_01…06 — блок unittest (01 → 06 за складністю)
test_pytest_01…06   — ті самі сценарії через pytest
"""

import os
import sys

# sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))
