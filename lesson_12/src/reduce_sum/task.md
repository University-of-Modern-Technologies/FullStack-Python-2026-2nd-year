Для списку `numbers` підрахуйте суму елементів за допомогою функції `reduce`.

```python
numbers = [3, 4, 6, 9, 34, 12]
```

Створіть функцію `sum_numbers(numbers)`, результатом виконання якої буде сума чисел усіх елементів
списку `numbers`.
